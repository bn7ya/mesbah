# feature: tasks (frontend)

`TasksPanel` — manage a project's objectives. Add row (title + objective) +
list with a status `p-select` and delete. Talks to `api.{listTasks,createTask,
updateTask,deleteTask}`. Smallest panel; mirror the backend `tasks` feature.
